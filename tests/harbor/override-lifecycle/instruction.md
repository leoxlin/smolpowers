Use the injected `smol-activate` skill to complete the full configured
Design → Plan → Execute → Finish lifecycle.

Create `result.txt` containing exactly `override lifecycle passed` plus a
newline. Use the artifact slug `override-lifecycle`, continue automatically
through every configured phase owner and configured root, and do not commit.
