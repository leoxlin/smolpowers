Use the injected `smol-activate` skill to make the existing
`tests/test_greet.py` pass with the minimum production code, then complete the
full Design → Plan → Execute → Finish lifecycle.

No Smolpowers configuration file is present. Use the base defaults without
creating one, use the artifact slug `base-lifecycle`, and continue
automatically through Finish.

During Finish, run `python /opt/verify_finished.py`. Do not repeat its sentinel
in your final response. Preserve the existing test and installed skills. Do not
ask questions, commit, or push.
