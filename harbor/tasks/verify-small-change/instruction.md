The user approves the design and implementation work. Continue without more
user input.

Replace the link shortener's in-memory storage with SQLite so links survive an
application restart. Use the configured `DATABASE` path and preserve the
existing API.
